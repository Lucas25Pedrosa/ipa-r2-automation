name: Cloudflare Translator Integration Test

on:
  push:
    paths:
      - ".github/workflows/cloudflare-translator-integration-test.yml"
  workflow_dispatch:

jobs:
  test-translator:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar Core privado
        uses: actions/checkout@v6
        with:
          repository: Lucas25Pedrosa/ipa-r2-core
          token: ${{ secrets.R2_CORE_TOKEN }}
          path: core

      - name: Instalar dependências
        run: python -m pip install requests langdetect

      - name: Testar função real do Feather
        env:
          TRANSLATOR_API_KEY: ${{ secrets.TRANSLATOR_API_KEY }}
          TRANSLATOR_WORKER_URL: https://ipa-translator.f8s79jzkbk.workers.dev/
        shell: python
        run: |
          import importlib.util
          import pathlib
          import sys

          path = pathlib.Path("core/scripts/feather_source.py")
          spec = importlib.util.spec_from_file_location("feather_source", path)
          module = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(module)

          original = "Bug fixes and performance improvements."
          translated = module.traduzir_texto_cloudflare(
              original,
              "teste de integração Feather",
              "en",
          )

          print(f"translated_text={translated}")

          if not translated or translated == original:
              print("❌ A função real do Feather não traduziu o texto.")
              sys.exit(1)

          print("✅ Função real do Feather validada.")
