name: R2 Automation Healthcheck

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  healthcheck:
    name: Runner Healthcheck
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Verificar runner
        run: |
          echo "R2 Automation runner ativo."
          date -u
